{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset("css/form.css") }}">
{% endblock %}


{% block title %}Nouveau secrétaire{% endblock %}

{% block body %}
    {% include 'composants/header.html.twig' %}
    <div class="d-flex justify-content-center">
        {{ form_start(form, {'attr':{'class':'w-75 d-flex flex-column'}} ) }}

        {{ form_row(form.login) }}
        {{ form_row(form.password) }}
        <div class="mt-4 d-flex justify-content-center w-100 button">
            {{ form_row(form.Sauvegarder) }}
        </div>

        {{ form_end(form) }}
    </div>
    <div class="d-flex mt-4 justify-content-center w-100">
        <a class="w-50 btn btn-primary" href="{{ path('homepageStaff') }}">Retour</a>
    </div>

{% endblock %}
